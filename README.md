# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.


Atividades implementadas
Atividade 01

Criação do projeto React utilizando Vite.

Atividade 02

Instalação das dependências e execução do servidor de desenvolvimento com npm run dev.

Atividade 03

Criação do primeiro componente funcional chamado Saudacao.

Atividade 04

Criação do componente Cartao utilizando múltiplas tags JSX e elemento raiz único.

Atividade 05

Uso de expressões JavaScript dentro do JSX utilizando variáveis, operações matemáticas e funções.

Atividade 06

Aplicação de estilos inline utilizando style={{ }} e propriedades em camelCase.

Atividade 07

Separação dos estilos em arquivos CSS externos utilizando className.

Atividade 08

Criação do componente Cabecalho e composição da interface utilizando múltiplos componentes.

Atividade 09

Estudo do funcionamento do main.jsx, createRoot() e integração do React com o index.html.

Atividade 10

Desenvolvimento da mini aplicação final utilizando componentes reutilizáveis, JSX, CSS externo e estrutura React completa.